spring:
  application:
    name: service-order
  config:
    import:
      - "optional:classpath:/config/cloud.yml"
      - "optional:classpath:/config/database.yml"

  profiles:
    active: ${PROFILE:dev}